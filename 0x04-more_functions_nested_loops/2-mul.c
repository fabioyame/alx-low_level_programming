#include "main.h"
#include <stdio.h>

/**
 * mul - multiplier
 * @a: first integer to multiply
 * @b: second integer to multiply
 * Return: result of two
 */

int mul(int a, int b)
{
return (a * b);
}
