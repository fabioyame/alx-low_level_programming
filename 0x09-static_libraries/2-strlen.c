#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - main function
 * @s: data to check
 * Return: Always return 0
 *
 */

int _strlen(char *s)
{
return (strlen(s));
}
