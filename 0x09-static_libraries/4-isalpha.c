#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>


/**
 * _isalpha - main function
 * @c: character to compare
 * Return: 1 if true, 0 if false
 */

int _isalpha(int c)
{
return (isalpha(c));
}

